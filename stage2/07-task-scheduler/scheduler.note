# Project 7: Task Scheduler - Self-Assessment Q&A

## Q1: Why use time.Timer vs time.Sleep?

```go
// time.Sleep - BLOCKS, cannot be cancelled
time.Sleep(5 * time.Second)  

// time.Timer - Uses CHANNEL, can be cancelled via select
timer := time.NewTimer(5 * time.Second)
select {
case <-timer.C:        // Fires after delay
case <-ctx.Done():     // Can be cancelled!
    timer.Stop()       // Clean up resources
}
```

| Aspect | time.Sleep | time.Timer |
|--------|------------|------------|
| Cancellable | No | Yes (via select) |
| Resource | Blocks goroutine | Returns channel |
| Cleanup | None | Call Stop() |

---

## Q2: time.Ticker vs time.Timer Loop

```go
// Ticker - Built-in repeated firing
ticker := time.NewTicker(1 * time.Second)
for range ticker.C {
    doWork()
}

// Timer loop - Manual reset
timer := time.NewTimer(1 * time.Second)
for {
    <-timer.C
    doWork()
    timer.Reset(1 * time.Second)  // Must reset manually!
}
```

| Use Case | Choose |
|----------|--------|
| Fixed interval | Ticker |
| Variable delay (backoff) | Timer loop |
| One-shot | Timer |

---

## Q3: Goroutine Cleanup on Close()

```go
func (s *Scheduler) Close() error {
    s.cancel()   // 1. Cancel context → signals all goroutines
    s.wg.Wait()  // 2. Wait for all to exit
    return nil
}

// In each task goroutine:
for {
    select {
    case <-ctx.Done():  // Listen for cancel
        return          // Exit cleanly
    case <-ticker.C:
        // work
    }
}
```

What prevents leaks:
- context.Context propagates cancellation
- sync.WaitGroup ensures all exit before Close() returns
- Every goroutine has exit path via ctx.Done()

---

## Q4: Cron Next() Efficiency

Current: Iterates second-by-second (slow for far future times)

More efficient: Jump to next valid field value
```go
// Jump to next valid month first
// Then jump to next valid day
// Then hour, minute, second
```

Production libs like robfig/cron use this approach.

---

## Q5: Task Overlap

If task takes longer than interval:
- Executions queue up
- Can cause backpressure

Solutions:
```go
// Option 1: Skip if already running
var running int32
if !atomic.CompareAndSwapInt32(&running, 0, 1) {
    return  // Skip
}
defer atomic.StoreInt32(&running, 0)

// Option 2: Use worker pool
// Option 3: Cancel previous execution
```

---

# Step 7.4: Advanced Features Q&A

## Q1: sync.Cond vs Channels

| Use sync.Cond | Use Channel |
|--------------|-------------|
| Many waiters, broadcast needed | 1:1 communication |
| Condition may become true/false repeatedly | One-time signal |
| Working with mutex-protected data | Passing data, not just signals |

sync.Cond advantages:
- Reusable: Can wait again after waking
- No allocation: Channels create memory for each waiter
- Condition rechecking: Works with for loop pattern

---

## Q2: Why for loop, not if?

```go
// WRONG - spurious wakeup issue
if !completed {
    cond.Wait()  // May wake without Signal/Broadcast
}

// CORRECT - re-check after every wakeup
for !completed {
    cond.Wait()
}
```

"Spurious wakeups" can happen due to:
- OS-level interrupts
- Another goroutine signaled for different condition

---

## Q3: DistributedLock TTL

Without TTL:
  Worker A acquires lock → crashes → lock held FOREVER
  Other workers wait forever = DEADLOCK

With TTL (30s):
  Worker A acquires lock → crashes
  After 30s: lock expires automatically
  Worker B can acquire

---

## Q4: Heap vs Sorted Slice

Heap: O(log n) push/pop - only fixes one path
Sorted slice: O(n) insert - shifts all elements

---

## Q5: Retry Backoff

Fixed delay: Hammers server during outage
Exponential: Gives server time to recover

MaxDelay cap: Prevents unreasonable waits (e.g., 256s after 10 retries)
